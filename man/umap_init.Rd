% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umap_init.R
\name{umap_init}
\alias{umap_init}
\title{Precompute a reusable UMAP initialization}
\usage{
umap_init(
  x,
  distances = NULL,
  n_neighbors = NULL,
  n_components = 2L,
  metric = c("euclidean", "cosine", "correlation"),
  backend = NULL,
  seed = 4L,
  n.cores = NULL,
  graph_mode = c("fuzzy", "binary")
)
}
\arguments{
\item{x}{Numeric data, a KNN object, or an object returned by
\code{\link[=prepare_umap_knn]{prepare_umap_knn()}}.}

\item{distances}{Optional KNN distance matrix when \code{x} is an index matrix.}

\item{n_neighbors}{Number of non-self neighbors when \code{x} is data. \code{NULL}
uses the package's size-aware default.}

\item{n_components}{Output dimensionality.}

\item{metric}{Distance metric used only when KNN must be computed.}

\item{backend}{Initialization backend: \code{"cpu"}, \code{"cuda"}, or \code{"metal"}.}

\item{seed}{Random seed.}

\item{n.cores}{Number of CPU cores.}

\item{graph_mode}{UMAP graph weighting. \code{"fuzzy"} is the standard default;
\code{"binary"} requests the adjacency-only sensitivity graph.}
}
\value{
A \code{fastEmbedR_umap_initialization} object with \code{layout}, \code{prepared},
resolved \code{parameters}, and component timings. Pass the complete object to
\code{\link[=umap_knn]{umap_knn()}} or use its \code{layout} component directly for diagnostics.
}
\description{
\code{umap_init()} separates UMAP neighbor search, graph construction, and
initialization from stochastic optimization. The returned object can be
passed directly to \code{\link[=umap_knn]{umap_knn()}} so repeated or cross-backend runs reuse the
same KNN graph and initial coordinates.
}
\details{
For the fuzzy graph, CPU initialization is computed from the prepared CSR
graph, while native Metal or CUDA spectral kernels are used when requested
and available. Binary initialization is computed from the same unit-weight
CSR graph used by the optimizer, so its mathematical input is unchanged.
Explicit GPU requests fail when the corresponding native backend is
unavailable.

CUDA KNN input is materialized on the host only for this standalone
diagnostic API. The ordinary one-call CUDA UMAP path remains device
resident.
}
\examples{
x <- scale(as.matrix(iris[, 1:4]))
initialization <- umap_init(
    x,
    n_neighbors = 15,
    backend = "cpu",
    graph_mode = "fuzzy",
    seed = 1
)
layout <- umap_knn(initialization, backend = "cpu", seed = 1)
plot(layout, pch = 21, bg = iris$Species)
}
