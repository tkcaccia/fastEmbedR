% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsne_pca_init.R
\name{tsne_pca_init}
\alias{tsne_pca_init}
\title{Compute or reuse a PCA initialization for t-SNE}
\usage{
tsne_pca_init(
  data,
  n_components = 2L,
  seed = 4L,
  backend = NULL,
  n.cores = 1L,
  cache_file = NULL,
  force_recompute = FALSE
)
}
\arguments{
\item{data}{Numeric matrix/data frame or \code{float::float32} matrix with
observations in rows.}

\item{n_components}{Output dimensionality, usually \code{2}.}

\item{seed}{Random seed used by the truncated PCA subspace sketch.}

\item{backend}{Backend used for PCA when available: \code{"cpu"}, \code{"metal"},
or \code{"cuda"}.}

\item{n.cores}{Number of CPU cores used for CPU PCA initialization. Metal
and CUDA ignore this argument.}

\item{cache_file}{Optional \code{.rds} file path. If it exists and
\code{force_recompute = FALSE}, the saved initialization is loaded and
validated.}

\item{force_recompute}{If \code{TRUE}, ignore any existing cache and recompute.}
}
\value{
An initialization matrix suitable for \code{Y_init}. Float32 input is
returned as \code{float::float32} when the selected PCA backend preserves it.
}
\description{
\code{tsne_pca_init()} creates the small-scale PCA initialization used by
\code{\link[=tsne]{tsne()}} and \code{\link[=tsne_knn]{tsne_knn()}}. Supplying \code{cache_file} stores the result
as an RDS file; later calls with the same path reuse the saved matrix instead
of recomputing PCA. This is useful when comparing several KNN backends with
exactly the same initialization. For \code{backend = "cuda"}, fastEmbedR selects
its native float32 rSVD or RAPIDS RAFT TSVD according to matrix shape and
target rank. Metal uses fastEmbedR's native float32 block-subspace rSVD with
Metal centering/scaling and Metal Performance Shaders matrix products. CPU
uses the native float32 blocked rSVD implementation. The package does not
call Python or \code{reticulate} for PCA initialization, and unavailable
accelerators fail explicitly.
}
\examples{
init <- tsne_pca_init(as.matrix(iris[, 1:4]), seed = 1)
plot(init, pch = 21, bg = iris$Species)
}
