% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umap.R
\name{umap}
\alias{umap}
\title{Run opinionated fixed-policy UMAP from data or precomputed KNN}
\usage{
umap(
  data,
  n_neighbors = NULL,
  n_components = 2L,
  standardize = FALSE,
  pca_dims = NULL,
  metric = c("euclidean", "cosine", "correlation"),
  nn = NULL,
  seed = 4L,
  backend = NULL,
  n.cores = NULL,
  keep_knn = FALSE,
  graph_mode = c("fuzzy", "binary"),
  verbose = FALSE,
  landmarks = FALSE,
  transform_k = NULL
)
}
\arguments{
\item{data}{Numeric matrix/data frame, or a list containing KNN \code{indices}
and \code{distances}.}

\item{n_neighbors}{Number of non-self neighbors. \code{NULL} chooses the package
default for the data size.}

\item{n_components}{Output dimensionality. The CPU backend supports positive
dimensions, including three-dimensional embeddings. The current Metal and
CUDA optimizers support only \code{2L}.}

\item{standardize}{Center and scale columns before KNN when \code{data} is a
matrix. Defaults to \code{FALSE} so one-call results match a KNN object computed
from the supplied matrix.}

\item{pca_dims}{Optional PCA dimension before KNN.}

\item{metric}{KNN distance metric for one-call matrix input: \code{"euclidean"},
\code{"cosine"}, or \code{"correlation"}.}

\item{nn}{Optional precomputed KNN result when \code{data} is a matrix.}

\item{seed}{Random seed.}

\item{backend}{Execution backend: \code{"cpu"}, \code{"cuda"}, or \code{"metal"}. CPU KNN
uses package-native exact search below 5,000 rows and HNSW otherwise.
Metal uses package-native exact or recall-tuned IVF-Flat search. CUDA uses
cuVS brute-force exact search below 100,000 rows and cuVS IVF-Flat above
that threshold. The KNN result stays on the device through graph
construction and optimization.
GPU requests must resolve to a real native backend; the package does not
relabel CPU work as GPU.}

\item{n.cores}{Requested CPU core count. For matrix input, the value is
passed to native CPU KNN and CPU UMAP; UMAP currently uses at most four
workers and records both requested and effective values. \code{NULL} uses one
KNN worker and a size-aware UMAP default from one to four workers. Native
GPU stages ignore this argument.}

\item{keep_knn}{Keep KNN matrices in the returned object.}

\item{graph_mode}{Graph weighting mode. \code{"fuzzy"} (the default) uses the
standard UMAP fuzzy graph. \code{"binary"} uses a symmetric unit-weight graph
as an adjacency-only sensitivity approximation; it is not standard UMAP
or necessarily faster.}

\item{verbose}{Print progress.}

\item{landmarks}{\code{FALSE} (the default) embeds all observations. A fraction
in \verb{(0, 1)}, a positive landmark count, or explicit row indices enables
landmark embedding followed by fixed-reference projection of the remaining
rows.}

\item{transform_k}{Number of landmark neighbors used to project non-landmark
observations. Used only when \code{landmarks} enables landmarking and defaults
to \code{n_neighbors}.}
}
\value{
A \code{fastEmbedR_embedding} object. Landmark fits also contain \code{model},
which can be passed to \code{\link[=project_landmark_model]{project_landmark_model()}} for new observations.
}
\description{
\code{umap()} is a small convenience wrapper. If \code{data} is already a list of KNN
\code{indices} and \code{distances}, it calls \code{\link[=umap_knn]{umap_knn()}} directly. Otherwise it
preprocesses the data, computes KNN once with fastEmbedR's native backend,
and embeds from that KNN graph.
}
\details{
\code{umap()} is an opinionated high-throughput UMAP implementation rather than
a drop-in interface for every UMAP hyperparameter. The public matrix-input
API exposes neighborhood size, distance metric, graph weighting, output
dimension, preprocessing, backend, seed, and CPU thread count. In the
current release, epochs, minimum distance, spread, learning rate, repulsion
strength, negative-sample rate, spectral iteration count, and asynchronous
update mode are selected internally. The resolved values and rule names are
stored in \code{fit$parameters}.

The default policy uses 500 epochs below 10,000 observations and 200 epochs
for larger inputs, increasing to 300 for high-variability distance profiles;
five negative samples, spread 1, and repulsion strength 1 are fixed. Minimum
distance is normally 0.01 and becomes 0.1 only under the documented
wide-shell profile; learning rate is correspondingly 1 or 1.25. Use
\code{\link[=umap_init]{umap_init()}} to precompute and reuse package-native initial coordinates,
or \code{\link[=umap_knn]{umap_knn()}} to supply an externally controlled KNN graph. These controls
are scientifically consequential; fixing them is a scope
decision made to keep one tested objective and update schedule aligned across
CPU, Metal, and CUDA, not a claim that the values are universally optimal.
Arbitrary \code{min_dist}, \code{spread}, epoch, learning-rate, negative-sampling, or
optimizer-mode sweeps should use a
general-purpose UMAP implementation; fastEmbedR does not claim API or
parameter interchangeability with those packages.

Landmark mode fits the requested fuzzy or binary UMAP graph on the
landmark rows, projects the remaining rows with query-to-reference KNN, and
optionally refines only projected rows while landmark coordinates remain
fixed. Precomputed KNN input cannot be combined with landmarking.
The returned \code{model} retains the fitted preprocessing transform so new data
can be supplied in the same original feature space.
}
\examples{
x <- scale(as.matrix(iris[, 1:4]))
fit <- umap(x, n_neighbors = 15, seed = 1)
plot(fit)
}
