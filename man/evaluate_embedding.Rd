% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_embedding.R
\name{evaluate_embedding}
\alias{evaluate_embedding}
\title{Evaluate an embedding against high-dimensional structure}
\usage{
evaluate_embedding(
  x_high,
  embedding,
  labels = NULL,
  batch = NULL,
  k = c(15L, 30L, 50L),
  primary_k = NULL,
  reference_nn = NULL,
  sample_size_for_global_metrics = min(5000L, nrow(x_high)),
  sample_size_for_local_metrics = min(2000L, nrow(x_high)),
  use_cache = FALSE,
  cache_dir = file.path("results", "cache"),
  force_recompute = FALSE,
  seed = NA_integer_,
  method = NA_character_,
  backend = NA_character_,
  n.cores = NULL,
  dataset = NA_character_
)
}
\arguments{
\item{x_high}{High-dimensional input, usually scaled data or PCA scores.}

\item{embedding}{Low-dimensional embedding matrix.}

\item{labels}{Optional biological/class labels.}

\item{batch}{Optional batch labels.}

\item{k}{Neighbor sizes used for structure metrics.}

\item{primary_k}{Neighbor size used for scalar trustworthiness, continuity,
label accuracy, rare-class recall, density summaries, and rank-error
metrics. The default \code{NULL} preserves the historical k = 15 behavior when
possible.}

\item{reference_nn}{Optional precomputed high-dimensional nearest-neighbor
list. It may include a self-neighbor column.}

\item{sample_size_for_global_metrics}{Maximum deterministic subsample size
for all-pairs global distance metrics.}

\item{sample_size_for_local_metrics}{Maximum deterministic subsample size for
local trustworthiness, continuity, and neighbor preservation metrics.}

\item{use_cache}{Cache the high-dimensional reference neighbors used by
quality metrics.}

\item{cache_dir}{Directory for quality-metric reference-neighbor cache files.}

\item{force_recompute}{Ignore cached quality reference neighbors.}

\item{seed}{Random seed recorded in the output and used for subsampling.}

\item{method}{Method name recorded in the output.}

\item{backend}{Backend name recorded in the output.}

\item{n.cores}{Number of CPU cores used when quality metrics need
nearest-neighbor searches. Ignored by native GPU metric backends.}

\item{dataset}{Dataset name recorded in the output.}
}
\value{
A one-row data frame with local, global, label-aware, batch-aware,
and metadata columns. Per-class recall is also attached as an attribute.
}
\description{
Trustworthiness and continuity use their standard rank-penalty definitions.
Local rank metrics are computed exactly on a deterministic subsample so the
function does not allocate all-pairs matrices for the complete dataset.
Float32 inputs are sampled before conversion to a dense double matrix.
}
\examples{
x <- scale(as.matrix(iris[, 1:4]))
metrics <- evaluate_embedding(
    x, x[, 1:2],
    labels = iris$Species, k = 5,
    sample_size_for_local_metrics = 100,
    sample_size_for_global_metrics = 100,
    seed = 1
)
}
