% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_knn_tsne.R
\name{tsne}
\alias{tsne}
\title{Run native interpolation-based t-SNE from a data matrix}
\usage{
tsne(
  data,
  perplexity = NULL,
  n_components = 2L,
  init_data = NULL,
  Y_init = NULL,
  standardize = FALSE,
  pca_dims = NULL,
  metric = c("euclidean", "cosine", "correlation"),
  nn = NULL,
  seed = 4L,
  backend = NULL,
  keep_knn = FALSE,
  verbose = FALSE,
  n.cores = NULL,
  learning_rate = "auto",
  early_exaggeration_iter = NULL,
  early_exaggeration = "auto",
  n_iter = NULL,
  exaggeration = NULL,
  initial_momentum = 0.8,
  final_momentum = 0.8,
  max_step_norm = "auto",
  negative_gradient_method = "auto",
  record_costs = FALSE,
  auto_config = TRUE,
  landmarks = FALSE,
  transform_k = NULL,
  transform_perplexity = 5,
  transform_iter = 250L,
  transform_early_exaggeration_iter = 0L,
  transform_n_negatives = NULL,
  initialization = c("median", "weighted", "random"),
  ...
)
}
\arguments{
\item{data}{Numeric matrix/data frame with observations in rows, or a list
containing KNN \code{indices} and \code{distances}.}

\item{perplexity}{t-SNE perplexity. If \code{NULL}, uses the largest safe value
up to 30 that is available for the input. Compact affinity support uses
\code{ceiling(perplexity)} non-self neighbors.}

\item{n_components}{Output dimensionality, from 1 to 3. Dimensions other
than two use CPU exact repulsion; the current Metal and CUDA
interpolation/FFT optimizers support only \code{2L}.}

\item{init_data}{Optional original high-dimensional data matrix used only to
compute PCA initialization. It is not used for
neighbor search or optimization.}

\item{Y_init}{Optional explicit initial layout. Use
\code{pca(data, tsne_init = TRUE)$tsne_init} to precompute and reuse a PCA
initialization.}

\item{standardize}{Center and scale columns before KNN. Defaults to \code{FALSE}
so one-call results match a KNN object computed from the supplied matrix.}

\item{pca_dims}{Optional PCA dimension before KNN.}

\item{metric}{KNN distance metric for one-call matrix input: \code{"euclidean"},
\code{"cosine"}, or \code{"correlation"}.}

\item{nn}{Optional precomputed KNN output when \code{data} is a data matrix.}

\item{seed}{Random seed.}

\item{backend}{Execution backend: \code{"cpu"}, \code{"cuda"}, or \code{"metal"}. CPU KNN
uses package-native exact search below 5,000 rows and HNSW otherwise.
Metal uses package-native exact or recall-tuned IVF-Flat search. CUDA uses
cuVS brute-force exact search below 100,000 rows and cuVS IVF-Flat above
that threshold. Device pointers pass directly to the native CUDA t-SNE
optimizer.
Unsupported GPU requests fail clearly and are not relabelled CPU runs.}

\item{keep_knn}{If \code{TRUE}, retain KNN matrices in the returned object.}

\item{verbose}{Print optimizer progress.}

\item{n.cores}{Number of CPU cores used by CPU KNN and CPU t-SNE
optimization. \code{NULL} uses one CPU KNN worker and the package t-SNE thread
option, which defaults to four. Native GPU optimizers ignore this
argument.}

\item{learning_rate}{Positive number or \code{"auto"}. With \code{"auto"}, the native
optimizer uses \code{n / exaggeration} separately for each phase.}

\item{early_exaggeration_iter}{Number of early-exaggeration iterations.}

\item{early_exaggeration}{Early-exaggeration multiplier, or \code{"auto"} for
the default t-SNE rule.}

\item{n_iter}{Number of normal optimization iterations after early
exaggeration.}

\item{exaggeration}{Normal-phase exaggeration. \code{NULL} means 1.}

\item{initial_momentum}{Momentum during early exaggeration.}

\item{final_momentum}{Momentum during normal optimization.}

\item{max_step_norm}{Maximum per-point update norm. \code{"auto"} uses the
same validated limit on CPU, Metal, and CUDA. Use \code{NULL} or \code{NA} to
disable clipping.}

\item{negative_gradient_method}{\code{"auto"}, \code{"exact"}, or
\code{"fft"}. On CPU, \code{"auto"} resolves to the native grid-FFT
FIt-SNE-style negative-gradient approximation. Native GPU FFT/exact paths
are used only when the corresponding compiled symbols are available;
otherwise GPU requests fail clearly rather than falling back to CPU.}

\item{record_costs}{If \code{TRUE}, compute diagnostic KL/cost traces.}

\item{auto_config}{If \code{TRUE}, choose missing t-SNE settings with a native
C++ opt-SNE-inspired policy. The policy uses \code{n / early_exaggeration} for
\code{"auto"} learning rate, chooses missing iteration limits, and enables
KLD-based early stopping only on CPU/small exact runs where the monitor is
not prohibitively expensive. Explicit user-supplied values are respected.}

\item{landmarks}{\code{FALSE} (the default) embeds all observations. A fraction
in \verb{(0, 1)}, a positive landmark count, or explicit row indices enables
landmark embedding followed by fixed-reference transformation of the
remaining rows.}

\item{transform_k}{Number of landmark neighbors used to place non-landmark
observations. Used only when landmarking is enabled.}

\item{transform_perplexity}{Perplexity used to transform non-landmark rows.}

\item{transform_iter}{Number of normal transform iterations. Use zero for
projection without transform refinement.}

\item{transform_early_exaggeration_iter}{Number of transform
early-exaggeration iterations.}

\item{transform_n_negatives}{Number of sampled reference negatives used by
large fixed-reference transforms.}

\item{initialization}{Initial placement for transformed rows: \code{"median"},
\code{"weighted"}, or \code{"random"}.}

\item{...}{Additional low-level parameters passed to \code{\link[=tsne_knn]{tsne_knn()}}.}
}
\value{
A \code{fastEmbedR_embedding} object. Landmark fits also contain \code{model},
which can be passed to \code{\link[=project_landmark_model]{project_landmark_model()}} for new observations.
}
\description{
\code{tsne()} computes or reuses a KNN graph, then runs the package-native
two-phase t-SNE optimizer. The default optimizer is CPU-native.
Explicit \code{backend = "metal"} and \code{backend = "cuda"} requests use the
matching package-native GPU optimizer when compiled and fail clearly if the
requested backend is unavailable.
The implementation follows published sparse-affinity and interpolation-based
t-SNE methods and provides independent CPU, Metal, and CUDA kernels.
}
\details{
The t-SNE API exposes the principal
scientifically consequential optimizer controls: perplexity,
initialization, iteration counts, early and normal exaggeration,
learning rate, momentum, clipping, and exact-versus-FFT repulsion. Setting
\code{auto_config = FALSE} disables automatic iteration and stopping choices;
explicit values always override automatic values. The matrix-input function
deliberately does not expose a nearest-neighbor index type or tuning
parameters: it uses the backend router with target recall 0.99. Supply an
externally generated KNN object to \code{\link[=tsne_knn]{tsne_knn()}} when search policy must be
controlled independently. The resolved settings are returned in
\code{fit$parameters}. Affinity construction always uses compact support with
\code{k = ceiling(perplexity)} non-self neighbors. Landmark mode embeds the
selected reference rows with the same t-SNE implementation and transforms
all remaining rows while keeping the reference coordinates fixed.
Precomputed KNN input and explicit \code{init_data} or \code{Y_init} cannot be combined
with landmarking.
}
\examples{
fit <- tsne(
    as.matrix(iris[, 1:4]),
    perplexity = 5,
    early_exaggeration_iter = 5, n_iter = 10, seed = 1
)
plot(fit, labels = iris$Species)
}
