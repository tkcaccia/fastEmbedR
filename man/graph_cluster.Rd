% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{graph_cluster}
\alias{graph_cluster}
\title{Native graph community detection}
\usage{
graph_cluster(
  graph,
  method = c("leiden", "louvain", "walktrap"),
  backend = NULL,
  resolution = 1,
  n_iterations = 10L,
  n_runs = 1L,
  steps = 4L,
  seed = 1L
)
}
\arguments{
\item{graph}{A \code{fastEmbedR_graph}, or an edge-list list containing \code{from},
\code{to}, \code{weight}, and \code{n_vertices}.}

\item{method}{\code{"leiden"}, \code{"louvain"}, or \code{"walktrap"}. Walktrap is the
package's true random-walk community method.}

\item{backend}{Execution backend. \code{"cuda"} and \code{"metal"} are available for
Louvain and Leiden only and fail explicitly if the requested accelerator
was not compiled or is unavailable. They never fall back to CPU.}

\item{resolution}{Positive modularity resolution for Louvain and Leiden.
Walktrap uses its reference resolution of 1.}

\item{n_iterations}{Maximum local-moving passes for Louvain and Leiden.}

\item{n_runs}{Independent seeded Louvain/Leiden runs; the highest-modularity
result is returned.}

\item{steps}{Random-walk length for Walktrap.}

\item{seed}{Reproducible random seed for Louvain and Leiden.}
}
\value{
A \code{fastEmbedR_graph_cluster} containing one-based \code{membership},
modularity, method, implementation, and run metadata.
}
\description{
\code{graph_cluster()} clusters a compact \code{fastEmbedR_graph} with a native
multilevel Louvain implementation, a Leiden local-move/refine/aggregate
implementation, or the Pons-Latapy Walktrap algorithm. Louvain and Leiden
have CPU, CUDA, and Metal implementations. Walktrap is CPU-only. No Python,
\code{igraph}, cuGraph, or external clustering function is called at run time.
}
\examples{
x <- scale(as.matrix(iris[, 1:4]))
graph <- knn_graph(x, k = 15)
clusters <- graph_cluster(graph, method = "leiden", seed = 1)
table(clusters$membership)
}
\references{
Blondel VD, Guillaume JL, Lambiotte R, Lefebvre E. Fast unfolding of
communities in large networks. J Stat Mech. 2008;2008:P10008.

Traag VA, Waltman L, van Eck NJ. From Louvain to Leiden: guaranteeing
well-connected communities. Sci Rep. 2019;9:5233.

Pons P, Latapy M. Computing communities in large networks using random
walks. J Graph Algorithms Appl. 2006;10:191-218.
}
