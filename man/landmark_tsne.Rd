% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_tsne.R
\name{landmark_tsne}
\alias{landmark_tsne}
\title{Landmark t-SNE with openTSNE-style transform}
\usage{
landmark_tsne(
  data,
  landmarks = TRUE,
  reference_method = c("opentsne"),
  n_neighbors = NULL,
  perplexity = NULL,
  n_components = 2L,
  standardize = TRUE,
  pca_dims = NULL,
  seed = 4L,
  backend = c("cpu", "cuda", "metal"),
  transform_k = NULL,
  transform_perplexity = 5,
  transform_iter = 250L,
  transform_early_exaggeration_iter = 0L,
  transform_n_negatives = NULL,
  initialization = c("median", "weighted", "random"),
  keep_knn = FALSE,
  verbose = FALSE,
  n_threads = NULL,
  ...
)
}
\arguments{
\item{data}{Numeric matrix/data frame with observations in rows.}

\item{landmarks}{`TRUE` for an automatic subset, a fraction such as `0.5`, a
landmark count, or explicit row indices.}

\item{reference_method}{Kept for compatibility. Only `"opentsne"` is
accepted in the cleaned package API.}

\item{n_neighbors}{Number of non-self neighbors. If `NULL`, the package uses
`3 * perplexity`, matching the usual t-SNE neighbour convention.}

\item{perplexity}{t-SNE perplexity. If `NULL`, uses
`min(30, floor((n - 1) / 3), floor(k / 3))`.}

\item{n_components}{Output dimensionality, from 1 to 3.}

\item{standardize}{Center and scale columns before KNN.}

\item{pca_dims}{Optional PCA dimension before KNN.}

\item{seed}{Random seed.}

\item{backend}{Execution backend: `"cpu"`, `"cuda"`, or `"metal"`.}

\item{transform_k}{Number of landmark neighbors used to place non-landmarks.}

\item{transform_perplexity}{Perplexity used by `transform_tsne()`.}

\item{transform_iter}{Number of normal transform iterations. Use `0` for
projection-only landmarking with no transform refinement.}

\item{transform_early_exaggeration_iter}{Number of transform early
exaggeration iterations.}

\item{transform_n_negatives}{Number of sampled reference negatives used by
`transform_tsne()` on large landmark sets. GPU sampled transform repulsion
is native and experimental for large reference sets.}

\item{initialization}{Initial placement for transformed observations.}

\item{keep_knn}{If `TRUE`, retain KNN matrices in the returned object.}

\item{verbose}{Print optimizer progress.}

\item{n_threads}{Number of CPU worker threads used by CPU KNN and CPU
transform optimization. Native GPU stages ignore this argument.}

\item{...}{Additional low-level parameters passed to [opentsne_knn()].}
}
\value{
A `fastEmbedR_embedding` object.
}
\description{
`landmark_tsne()` embeds a subset of observations with [opentsne()], then
places the remaining observations with `transform_tsne()`. This mirrors the
practical landmark workflow exposed by openTSNE while keeping the
implementation native to this package. On Apple Silicon, `backend = "metal"`
uses a fused native Metal projection kernel for query-to-landmark KNN,
interpolation, and projection confidence before the fixed-reference
transform.
}
