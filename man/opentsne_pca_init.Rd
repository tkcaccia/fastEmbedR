% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_knn_tsne.R
\name{opentsne_pca_init}
\alias{opentsne_pca_init}
\title{Compute or reuse a PCA initialization for openTSNE}
\usage{
opentsne_pca_init(
  data,
  n_components = 2L,
  seed = 4L,
  backend = c("cpu", "metal", "cuda"),
  cache_file = NULL,
  force_recompute = FALSE
)
}
\arguments{
\item{data}{Numeric matrix/data frame with observations in rows.}

\item{n_components}{Output dimensionality, usually `2`.}

\item{seed}{Random seed used by the randomized SVD PCA helper.}

\item{backend}{Backend used for PCA when available: `"cpu"`, `"metal"`,
or `"cuda"`.}

\item{cache_file}{Optional `.rds` file path. If it exists and
`force_recompute = FALSE`, the saved initialization is loaded and
validated.}

\item{force_recompute}{If `TRUE`, ignore any existing cache and recompute.}
}
\value{
A numeric initialization matrix suitable for `Y_init`.
}
\description{
`opentsne_pca_init()` creates the small-scale PCA initialization used by
[opentsne()] and [opentsne_knn()]. Supplying `cache_file` stores the result
as an RDS file; later calls with the same path reuse the saved matrix instead
of recomputing PCA. This is useful when comparing several KNN backends with
exactly the same initialization.
}
