% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_knn_tsne.R
\name{opentsne}
\alias{opentsne}
\title{Run native openTSNE-style t-SNE from a data matrix}
\usage{
opentsne(
  data,
  labels = NULL,
  n_neighbors = NULL,
  perplexity = NULL,
  n_components = 2L,
  standardize = TRUE,
  pca_dims = NULL,
  nn = NULL,
  seed = 4L,
  backend = c("auto", "cpu", "gpu", "metal", "cuda", "cuvs", "gpu_cuvs", "cuda_cuvs",
    "cuda_cuvs_cagra", "cuda_cuvs_bruteforce", "cuda_cuvs_exact", "cuda_cuvs_nndescent",
    "cuvs_bruteforce", "cuvs_nndescent"),
  silhouette_sample = NULL,
  preserve_sample = NULL,
  preserve_k = NULL,
  keep_knn = FALSE,
  verbose = FALSE,
  n_threads = NULL,
  learning_rate = "auto",
  early_exaggeration_iter = NULL,
  early_exaggeration = "auto",
  n_iter = NULL,
  exaggeration = NULL,
  initial_momentum = 0.8,
  final_momentum = 0.8,
  max_step_norm = "auto",
  negative_gradient_method = "auto",
  record_costs = FALSE,
  auto_config = TRUE,
  ...
)
}
\arguments{
\item{data}{Numeric matrix/data frame with observations in rows, or a KNN
object returned by [nn()].}

\item{labels}{Optional labels used only for scoring and plotting metadata.}

\item{n_neighbors}{Number of non-self neighbors. If `NULL`, the package uses
`3 * perplexity`, matching the usual t-SNE neighbour convention.}

\item{perplexity}{t-SNE perplexity. If `NULL`, uses
`min(30, floor((n - 1) / 3), floor(k / 3))`.}

\item{n_components}{Output dimensionality, from 1 to 3.}

\item{standardize}{Center and scale columns before KNN.}

\item{pca_dims}{Optional PCA dimension before KNN.}

\item{nn}{Optional precomputed KNN output when `data` is a data matrix.}

\item{seed}{Random seed.}

\item{backend}{KNN/preprocessing backend and, when the matching native
openTSNE symbols are compiled, a real GPU optimizer backend. Unsupported
GPU requests fail clearly and are not relabelled CPU runs. `backend =
"auto"` keeps the CPU optimizer.}

\item{silhouette_sample}{Optional sample size for silhouette scoring.}

\item{preserve_sample}{Optional sample size for neighborhood scoring.}

\item{preserve_k}{Number of neighbors used for neighborhood scoring.}

\item{keep_knn}{If `TRUE`, retain KNN matrices in the returned object.}

\item{verbose}{Print optimizer progress.}

\item{n_threads}{Number of CPU worker threads used by CPU KNN and CPU
openTSNE optimization. Native GPU optimizers ignore this argument.}

\item{learning_rate}{Positive number or `"auto"`. With `"auto"`, the native
optimizer uses `n / exaggeration` separately for each phase.}

\item{early_exaggeration_iter}{Number of early-exaggeration iterations.}

\item{early_exaggeration}{Early-exaggeration multiplier, or `"auto"` for
openTSNE's default rule.}

\item{n_iter}{Number of normal optimization iterations after early
exaggeration.}

\item{exaggeration}{Normal-phase exaggeration. `NULL` means 1.}

\item{initial_momentum}{Momentum during early exaggeration.}

\item{final_momentum}{Momentum during normal optimization.}

\item{max_step_norm}{Maximum per-point update norm. `"auto"` uses the
standard CPU limit and a tighter native Metal FFT-grid limit to avoid
float32 outlier steps. Use `NULL` or `NA` to disable clipping.}

\item{negative_gradient_method}{`"auto"`, `"exact"`, or
`"fft"`. On CPU, `"auto"` resolves to the native grid-FFT
FIt-SNE-style negative-gradient approximation. Native GPU FFT/exact paths
are used only when the corresponding compiled symbols are available;
otherwise GPU requests fail clearly rather than falling back to CPU.}

\item{record_costs}{If `TRUE`, compute diagnostic KL/cost traces.}

\item{auto_config}{If `TRUE`, choose missing t-SNE settings with a native
C++ opt-SNE-inspired policy. The policy uses `n / early_exaggeration` for
`"auto"` learning rate, chooses missing iteration limits, and enables
KLD-based early stopping only on CPU/small exact runs where the monitor is
not prohibitively expensive. Explicit user-supplied values are respected.}

\item{...}{Additional low-level parameters passed to [opentsne_knn()].}
}
\value{
A `fastEmbedR_embedding` object.
}
\description{
`opentsne()` computes or reuses a KNN graph, then runs the package-native
openTSNE-style two-phase optimizer. The default optimizer is CPU-native.
Explicit `backend = "metal"` and `backend = "cuda"` requests use the
matching package-native GPU optimizer when compiled and fail clearly if the
requested backend is unavailable.
}
