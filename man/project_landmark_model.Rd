% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmark_model.R
\name{project_landmark_model}
\alias{project_landmark_model}
\title{Project observations into a fitted landmark embedding}
\usage{
project_landmark_model(
  model,
  data,
  query_indices = NULL,
  transform_k = NULL,
  refinement_epochs = 50L,
  transform_perplexity = 5,
  transform_iter = 250L,
  transform_early_exaggeration_iter = 0L,
  transform_n_negatives = NULL,
  initialization = c("median", "weighted", "random"),
  keep_knn = FALSE,
  n.cores = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{model}{The \code{model} element returned by \code{\link[=umap]{umap()}} or \code{\link[=tsne]{tsne()}} when
landmarking is enabled.}

\item{data}{New query observations in the original feature space used for
fitting. The stored standardization and PCA transforms are applied before
query-to-reference search.}

\item{query_indices}{Optional rows of \code{data} to project. To reconstruct the
original training layout explicitly, pass the original matrix and
\code{model$selection$query_indices}. With \code{NULL}, every supplied row is treated
as a genuinely new query, even when its row count matches the training set.}

\item{transform_k}{Number of reference neighbors.}

\item{refinement_epochs}{Fixed-reference UMAP refinement epochs.}

\item{transform_perplexity}{Perplexity of the t-SNE transform.}

\item{transform_iter}{Number of t-SNE transform iterations.}

\item{transform_early_exaggeration_iter}{Transform exaggeration iterations.}

\item{transform_n_negatives}{Number of reference negatives for t-SNE.}

\item{initialization}{t-SNE query initialization.}

\item{keep_knn}{Retain query-to-reference KNN output.}

\item{n.cores}{Number of CPU cores.}

\item{verbose}{Print optimizer progress.}

\item{...}{Low-level t-SNE transform controls.}
}
\value{
A \code{fastEmbedR_embedding}. Explicit original-query indices reassemble
\code{layout} in training row order; otherwise it contains only query rows. The
returned
parameters record \code{projection_scope} as \code{"original_reconstruction"} or
\code{"held_out_query"}.
}
\description{
\code{project_landmark_model()} computes query-to-reference KNN with the same
native backend policy used by the full embedding functions, then applies the
method-specific fixed-reference transform. Landmark coordinates remain
fixed.
}
\examples{
x <- scale(as.matrix(iris[, 1:4]))
fit <- umap(x, n_neighbors = 10, landmarks = 0.5, seed = 1)
query <- x[1:3, , drop = FALSE]
projected <- project_landmark_model(
    fit$model, query, transform_k = 10, refinement_epochs = 0
)
}
