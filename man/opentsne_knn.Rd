% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_knn_tsne.R
\name{opentsne_knn}
\alias{opentsne_knn}
\title{Run native openTSNE-style t-SNE from precomputed KNN}
\usage{
opentsne_knn(
  indices,
  distances = NULL,
  n_neighbors = NULL,
  perplexity = NULL,
  n_components = 2L,
  seed = 4L,
  verbose = FALSE,
  backend = c("auto", "cpu", "gpu", "metal", "cuda"),
  n_threads = NULL,
  learning_rate = "auto",
  early_exaggeration_iter = 250L,
  early_exaggeration = "auto",
  n_iter = 500L,
  exaggeration = NULL,
  initial_momentum = 0.8,
  final_momentum = 0.8,
  max_step_norm = "auto",
  negative_gradient_method = "auto",
  record_costs = FALSE,
  ...
)
}
\arguments{
\item{indices}{A KNN object returned by [nn()], or an integer KNN index
matrix.}

\item{distances}{Numeric KNN distance matrix matching `indices`. Leave
`NULL` when `indices` is an [nn()] result.}

\item{n_neighbors}{Optional number of non-self neighbor columns to use from
the supplied KNN graph. This lets you compute a wide KNN once and reuse
its first columns for comparable tests.}

\item{perplexity}{t-SNE perplexity. If `NULL`, uses
`min(30, floor((n - 1) / 3), floor(k / 3))`.}

\item{n_components}{Output dimensionality, from 1 to 3.}

\item{seed}{Random seed.}

\item{verbose}{Print optimizer progress.}

\item{backend}{KNN/preprocessing backend and, when the matching native
openTSNE symbols are compiled, a real GPU optimizer backend. Unsupported GPU
requests fail clearly and are not relabelled CPU runs. `backend = "auto"`
keeps the CPU optimizer.}

\item{n_threads}{Number of CPU worker threads used by CPU KNN and CPU
openTSNE optimization. Native GPU optimizers ignore this argument.}

\item{learning_rate}{Positive number or `"auto"`. With `"auto"`, the native
optimizer uses `n / exaggeration` separately for each phase.}

\item{early_exaggeration_iter}{Number of early-exaggeration iterations.}

\item{early_exaggeration}{Early-exaggeration multiplier, or `"auto"` for
openTSNE's default rule.}

\item{n_iter}{Number of normal optimization iterations after early
exaggeration.}

\item{exaggeration}{Normal-phase exaggeration. `NULL` means 1.}

\item{initial_momentum}{Momentum during early exaggeration.}

\item{final_momentum}{Momentum during normal optimization.}

\item{max_step_norm}{Maximum per-point update norm. Use `NULL` or `NA` to
disable clipping.}

\item{negative_gradient_method}{`"auto"`, `"exact"`,
or `"fft"`. On CPU, `"auto"` resolves to the native grid-FFT
FIt-SNE-style negative-gradient approximation. Native GPU FFT/exact paths are
used only when the corresponding compiled symbols are available; otherwise GPU
requests fail clearly rather than falling back to CPU.}

\item{record_costs}{If `TRUE`, compute diagnostic KL/cost traces.}

\item{...}{Additional low-level parameters passed to [opentsne_knn()].}
}
\value{
A numeric embedding matrix with settings stored in
  `attr(layout, "fastEmbedR_config")`.
}
\description{
`opentsne_knn()` is the direct KNN-input entry point for the native
openTSNE-style optimizer. It accepts either an object returned by [nn()] or
separate KNN index and distance matrices. No neighbour search, scaling, or
PCA is done inside this function.
}
\examples{
x <- scale(as.matrix(iris[, 1:4]))
knn <- nn(x, k = 31)
layout <- opentsne_knn(knn, perplexity = 10,
  early_exaggeration_iter = 100, n_iter = 250)
if (all(is.finite(layout))) {
  plot(layout, pch = 21, bg = iris$Species)
}
}
